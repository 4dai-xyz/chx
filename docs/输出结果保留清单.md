# 输出结果保留清单

本文记录当前 `output/` 目录清理后的正式结果。后续查看结果时，以本文为准。

## 1. 当前 output 目录只保留两类正式结果

```text
output/
├── dpvo_enhanced/
└── vggt_aligned_full_run/
```

已经删除的内容包括：

- DPVO 临时测试目录：`dpvo_enhanced_ros_test`、`dpvo_enhanced_visual_test`、`dpvo_enhanced_wrapper_test`
- VGGT 早期或烟雾测试目录：`vggt_input_video_show`、`vggt_scene_full`、`vggt_smoke_aligned`
- 调试图片：`debug_overlay_mask.jpg`、`debug_slam_input.jpg`
- DPVO 重复 3D 实验目录：`visualization_3d`、`visualization_3d_aligned`、`visualization_3d_left_turn`、`visualization_3d_left_turn_mirror`

## 2. DPVO 正式保留结果

DPVO 正式目录：

```text
output/dpvo_enhanced/mall_dpvo
```

保留内容：

```text
output/dpvo_enhanced/mall_dpvo/summary.txt
output/dpvo_enhanced/mall_dpvo/summary.json
output/dpvo_enhanced/mall_dpvo/tracking_quality.csv
output/dpvo_enhanced/mall_dpvo/tracking_quality.json
output/dpvo_enhanced/mall_dpvo/tracking_quality.png
output/dpvo_enhanced/mall_dpvo/tracking_events.json
output/dpvo_enhanced/mall_dpvo/relocalization_candidates.json
output/dpvo_enhanced/mall_dpvo/relocalization_results.json
output/dpvo_enhanced/mall_dpvo/relocalization_results.csv
output/dpvo_enhanced/mall_dpvo/relocalization_report.txt
output/dpvo_enhanced/mall_dpvo/keyframes/
output/dpvo_enhanced/mall_dpvo/visualization/
output/dpvo_enhanced/mall_dpvo/visualization_3d_corrected/
```

当前只保留两个可视化结果文件夹：

```text
visualization/              原始二维增强结果
visualization_3d_corrected/ 最终校正后的三维视觉增强结果
```

原始二维增强视频：

```text
output/dpvo_enhanced/mall_dpvo/visualization/dpvo_enhanced_visualization.mp4
```

最终校正后的三维视觉增强视频：

```text
output/dpvo_enhanced/mall_dpvo/visualization_3d_corrected/dpvo_enhanced_3d.mp4
```

最终校正后的三维路线图：

```text
output/dpvo_enhanced/mall_dpvo/visualization_3d_corrected/dpvo_3d_route.png
output/dpvo_enhanced/mall_dpvo/visualization_3d_corrected/dpvo_top_route.png
```

已验证视频属性：

```text
二维增强视频: opened=True, frames=1590, fps=14.709, size=1420x700
三维增强视频: opened=True, frames=399, fps=12.0, size=1280x720
```

## 3. VGGT 正式保留结果

VGGT 只保留一个完整结果目录：

```text
output/vggt_aligned_full_run
```

这个目录是目前 VGGT 最完整的一套结果，包含：

```text
summary.txt
summary.json
camera_centers.txt
frame_meta.json
vggt_points.ply
aligned_full/aligned_full_scene.ply
aligned_full/aligned_camera_trajectory.json
aligned_colmap/
gsplat_results/
gsplat_results_30k/
windows/
```

最重要的 VGGT 全局对齐点云：

```text
output/vggt_aligned_full_run/aligned_full/aligned_full_scene.ply
```

如果后续需要查看 VGGT 的完整三维重建结果，优先看：

```text
output/vggt_aligned_full_run/aligned_full/
output/vggt_aligned_full_run/gsplat_results_30k/
```

## 4. 后续建议

如果只是查看 DPVO 增强效果：

```text
看 output/dpvo_enhanced/mall_dpvo/visualization/
看 output/dpvo_enhanced/mall_dpvo/visualization_3d_corrected/
```

如果只是查看 VGGT 完整结果：

```text
看 output/vggt_aligned_full_run/
```

如果后续重新跑实验，建议使用新的输出目录名，避免覆盖当前正式结果。
